# React Native / Expo needs a flat node_modules layout for Metro to resolve packages.
node-linker=hoisted
shamefully-hoist=true
strict-peer-dependencies=false

# Don't fail scripts just because build-scripts are pending approval.
# Approved builds are pinned in the root package.json under `pnpm.onlyBuiltDependencies`.
verify-deps-before-run=false
